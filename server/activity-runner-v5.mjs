import { runTurn } from "./harness.mjs";
import { buildSkillInvocation, loadMattSkillBundle } from "./skill-adapter.mjs";
import { newAgentSession, newSkillRun, BUILD_ROUTES } from "./domain-model.mjs";
import { getActivity, saveActivity, getWork, saveWork, saveAgentSession, saveSkillRun } from "./store-v5.mjs";
import { executeRecovery, planRecovery } from "./recovery-runner.mjs";

const ACTIVITY_PHASE = { alignment: "analysis", specification: "spec", planning: "spec", implementation: "impl", recovery: "impl" };
const ROUTE_MARKER = /ANVIL_ROUTE:\s*(\{[^\n]+\})\s*$/m;
function instructionsFor(activity) { const route = activity.type === "alignment" ? ` When the requirement is clear enough to recommend the next engineering shape, append exactly one single-line marker: ANVIL_ROUTE: {"route":"direct|spec|tickets","reason":"brief reason"}. Choose direct when this should stay in one coding-agent session without durable spec/ticket decomposition; spec when a durable implementation spec is useful but ticket decomposition is unnecessary; tickets only when decomposition/parallel or dependency-aware execution is genuinely useful. This marker is advisory for Anvil UI; do not force a route before the requirement is clear.` : ""; return `You are executing an Anvil engineering activity. Follow the requested Matt Pocock skill as the source of engineering method. Do not invent a parallel Anvil methodology. Current activity: ${activity.type}.${route}`; }
function appendConversation(activity, role, text) { const clean = String(text || "").trim(); if (!clean) return; activity.conversation ||= []; activity.conversation.push({ role, text: clean, at: new Date().toISOString() }); }
function extractRouteRecommendation(text) { const hit = String(text || "").match(ROUTE_MARKER); if (!hit) return { clean: String(text || "").trim(), recommendation: null }; try { const value = JSON.parse(hit[1]); if (!BUILD_ROUTES.has(value.route)) return { clean: String(text || "").replace(hit[0], "").trim(), recommendation: null }; return { clean: String(text || "").replace(hit[0], "").trim(), recommendation: { route: value.route, reason: String(value.reason || "").slice(0, 300), at: new Date().toISOString() } }; } catch { return { clean: String(text || "").replace(hit[0], "").trim(), recommendation: null }; } }

export async function continueActivitySession({ workId, activityId, project, prompt, phase = null, onEvent }) { const work = await getWork(workId); if (!work) throw new Error(`Work 不存在: ${workId}`); const activity = await getActivity(activityId); if (!activity || activity.workId !== workId) throw new Error(`Activity 不存在: ${activityId}`); if (!activity.sessionId) throw new Error(`Activity ${activityId} 尚未建立 Agent Session`); const skill = activity.technical?.skill; const skills = skill ? await loadMattSkillBundle(skill) : []; const run = newSkillRun({ activityId, sessionId: activity.sessionId, skill: skill || "session-continuation" }); run.status = "running"; run.startedAt = new Date().toISOString(); activity.status = "running"; work.currentActivityId = activity.id; work.status = "active"; await saveSkillRun(run); await saveActivity(activity); await saveWork(work); try { const out = await runTurn({ reqId: work.id, worktreesDir: project.worktreesDir, sessionKey: `activity-${activity.id}`, phase: phase || ACTIVITY_PHASE[activity.type] || "impl", instructions: `${instructionsFor(activity)} Continue the existing coding-agent session. Do not restart requirement discovery or create spec/tickets unless the task itself now requires them.`, skills, prompt, onEvent }); run.status = "completed"; run.summary = out.text.slice(0, 500); run.tools = out.tools.slice(0, 50); run.files = out.files; run.finishedAt = new Date().toISOString(); activity.finishedAt = run.finishedAt; await saveSkillRun(run); await saveActivity(activity); await saveWork(work); return { work, activity, run, output: out }; } catch (error) { run.status = "interrupted"; run.summary = error.message; run.finishedAt = new Date().toISOString(); await saveSkillRun(run); throw error; } }

export async function runActivity({ workId, activityId, project, prompt = "", onEvent }) {
  const work = await getWork(workId); if (!work) throw new Error(`Work 不存在: ${workId}`); const activity = await getActivity(activityId); if (!activity || activity.workId !== workId) throw new Error(`Activity 不存在: ${activityId}`); const skill = activity.technical?.skill; if (!skill) throw new Error(`Activity ${activityId} 没有关联 skill`);
  async function executeOnce({ prompt: nextPrompt = prompt, freshSession = false } = {}) { const previousSessionId = activity.sessionId; const session = newAgentSession({ workId, activityId, ticketId: activity.ticketId, continuationOf: freshSession ? previousSessionId : null }); const run = newSkillRun({ activityId, sessionId: session.id, skill }); if (activity.type === "alignment") appendConversation(activity, "user", nextPrompt); session.status = "active"; run.status = "running"; run.startedAt = new Date().toISOString(); activity.status = "running"; activity.sessionId = session.id; activity.startedAt ||= run.startedAt; work.currentActivityId = activity.id; work.status = "active"; await saveAgentSession(session); await saveSkillRun(run); await saveActivity(activity); await saveWork(work); const invocation = buildSkillInvocation(skill, nextPrompt); const skills = await loadMattSkillBundle(skill);
    try { const out = await runTurn({ reqId: work.id, worktreesDir: project.worktreesDir, sessionKey: freshSession ? `activity-${activity.id}-${session.id}` : `activity-${activity.id}`, phase: ACTIVITY_PHASE[activity.type] || "impl", instructions: instructionsFor(activity), skills, prompt: invocation, onEvent }); const parsed = activity.type === "alignment" ? extractRouteRecommendation(out.text) : { clean: out.text, recommendation: null }; run.status = "completed"; run.summary = parsed.clean.slice(0, 500); run.tools = out.tools.slice(0, 50); run.files = out.files; run.finishedAt = new Date().toISOString(); session.status = "completed"; if (activity.type === "alignment") { appendConversation(activity, "assistant", parsed.clean); if (parsed.recommendation) activity.routeRecommendation = parsed.recommendation; } if (activity.gate?.required) { activity.status = "waiting_user"; activity.gate.status = "waiting"; work.status = "waiting_user"; } else { activity.status = "completed"; work.status = "active"; } activity.finishedAt = new Date().toISOString(); await saveSkillRun(run); await saveAgentSession(session); await saveActivity(activity); await saveWork(work); return { work, activity, run, output: { ...out, text: parsed.clean } }; } catch (error) { run.status = "interrupted"; run.summary = error.message; run.finishedAt = new Date().toISOString(); session.status = "interrupted"; await saveSkillRun(run); await saveAgentSession(session); error.runId = run.id; throw error; }
  }
  try { return await executeOnce(); } catch (error) { const planned = await planRecovery({ error, work, activity, runId: error.runId }); if (planned.decision.action !== "recover") return { work, activity, interruption: planned.interruption, recovery: planned.decision }; const recovered = await executeRecovery({ work, activity, interruption: planned.interruption, project, runOriginal: executeOnce, onEvent }); if (recovered.recovered) return recovered.result; const next = await planRecovery({ error: recovered.error || error, work, activity, runId: error.runId }); return { work, activity, interruption: next.interruption, recovery: next.decision }; }
}