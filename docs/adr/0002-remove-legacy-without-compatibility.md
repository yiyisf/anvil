---
status: accepted
---

# Remove Legacy without compatibility

BUILD is the only supported product lifecycle. Legacy Requirement UI, execution, persistence access and migration code will be deleted rather than restored or preserved, because the project has no current requirement to read or migrate Legacy records; shared Project, Session, Agent, Sandbox and Worktree capabilities remain independent of Legacy.
